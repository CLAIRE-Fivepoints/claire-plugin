from pathlib import Path

plugin_info = {
    "name": "fivepoints",
    "docs_dir": Path(__file__).parent / "domain",
    "plugin_yaml": Path(__file__).parent / "plugin.yaml",
    "version": "0.1.0",
}
