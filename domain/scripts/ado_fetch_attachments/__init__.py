# ado_fetch_attachments — fetch FDS attachments from Azure DevOps,
# compare MD5 against the domain cache, extract images with section cross-references,
# and open a drift issue when the cache is stale.
